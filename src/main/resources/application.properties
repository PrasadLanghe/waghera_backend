spring.application.name=WagheraAgro

# ===============================================
# SERVER CONFIGURATION
# ===============================================
server.port=8080

# ===============================================
# MYSQL DATABASE CONFIGURATION
# IMPORTANT: Replace placeholders with your credentials
# ===============================================
spring.datasource.url=jdbc:mysql://localhost:3306/devcon?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================
# JPA / HIBERNATE CONFIGURATION
# ===============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================================
# CORS Configuration (To allow React Frontend)
# ===============================================
app.cors.allowed-origins=http://localhost:5173


# ===============================================
# EMAIL CONFIGURATION (Gmail SMTP)
# ===============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=langheps2003@gmail.com
spring.mail.password=fbgy bxvz xlrh uhzi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000






cloudinary.cloud_name=djw9hiima
cloudinary.api_key=221854977844961
cloudinary.api_secret=hW7p8MNJbtN96lWFE0Q17EJ_Qc0


# Enable multipart uploads (usually true by default, but good practice to include)
spring.servlet.multipart.enabled=true

# 1. Maximum size of a single file in the request (e.g., 50 Megabytes)
spring.servlet.multipart.max-file-size=50MB 

# 2. Maximum total size of the entire request (includes all files and other form data)
spring.servlet.multipart.max-request-size=50MB 

# 3. Optional: Set Tomcat-specific property (sometimes required for large uploads)
# This prevents Tomcat from prematurely rejecting the request if the size is too big.
server.tomcat.max-swallow-size=50MB
